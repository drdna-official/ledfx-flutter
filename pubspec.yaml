name: ledfx
description: "Record Audio Stream from your (Android/Windows) device and control WLED device"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.1
  http: ^1.5.0
  equatable: ^2.0.7
  nanoid: ^1.0.0
  shared_preferences: ^2.5.4
  flex_color_picker: ^3.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
